params {
  profile = false
  cache = ''
  outdir = 'results'
  qc_filename = 'qc.csv'
}

profiles {
  conda {
     process.conda = "$baseDir/environments/environment.yml"
     if (params.cache){
       conda.cacheDir = params.cache
     }
  }
}